*************************************************************************************************

常用 Git 命令

*************************************************************************************************

1、添加远程仓库,然后提交
   git remote add origin https://github.com/coolligang/WorkNote.git

   git init
   git add README.md
   git commit -m "first commit"
   git push -u origin master